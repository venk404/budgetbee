if [[ -z "$POSTGRES_SUBSCRIPTION_ADMIN_PASSWORD" ]]; then
    echo "POSTGRES_SUBSCRIPTION_ADMIN_PASSWORD is not set"
    exit 1
fi
psql -c "CREATE ROLE subscription_admin WITH LOGIN PASSWORD '${POSTGRES_SUBSCRIPTION_ADMIN_PASSWORD}' BYPASSRLS;"

if [[ -z "$POSTGRES_AUTH_ADMIN_PASSWORD" ]]; then
    echo "POSTGRES_AUTH_ADMIN_PASSWORD is not set"
    exit 1
fi
psql -c "CREATE ROLE auth_admin WITH LOGIN PASSWORD '${POSTGRES_AUTH_ADMIN_PASSWORD}' BYPASSRLS;"
