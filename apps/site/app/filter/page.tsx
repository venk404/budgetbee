"use client";

export default function Page() {
	/*
        React.useEffect(() => {
            authClient.token().then(x => console.log("token: ", x))
            db().from("transactions").select("*").then(x => console.log("transactions: ", x))
        }, []);
        */

	return <div>Filter</div>;
}
